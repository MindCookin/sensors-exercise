'use strict';

angular.
  module('uploadCsv').
  factory('upload', ['$resource',
    function($resource) {
      // return $resource('/files', {}, {
      //   query: {
      //     method: 'POST'
      //   }
      // });
    }
  ]);
