'use strict';

angular.
  module('dashboard').
  factory('dashboard', ['$resource',
    function($resource) {
      // return $resource('/files', {}, {
      //   query: {
      //     method: 'POST'
      //   }
      // });
    }
  ]);
